CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(TestSVD2NormCondNumb)

SET(MAIN_FILE "main")
SET(EXEC_FILE "testSVD2NormCondNumb")

INCLUDE(CTest)

ADD_TEST(NAME ${EXEC_FILE} COMMAND ${EXEC_FILE})

femusMacroBuildApplication(${MAIN_FILE} ${EXEC_FILE})