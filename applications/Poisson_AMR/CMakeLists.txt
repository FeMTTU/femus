CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(Poisson_AMR)

SET(DIR_NAME "Poisson_AMR")
SET(MAIN_FILE "main")
SET(EXEC_FILE "Poisson_AMR")

femusMacroBuildApplication( ${DIR_NAME} ${MAIN_FILE} ${EXEC_FILE})



